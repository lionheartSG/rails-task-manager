<h1>My tasks</h1>
  <% @tasks.each do |t| %>
  <li><%=check_box_tag t.title, t.title, t.completed%>
  <%=link_to t.title, task_path(t) %>
  </li>
  <% end %>
</ul>

<%=link_to "Create New Task", new_task_path, class: "btn btn-primary btn" %>
